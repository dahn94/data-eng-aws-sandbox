# bi-superset

Apache Superset local, alternativa ao Metabase pra dashboards de BI.

## Subir

```bash
cd local-services/bi-superset
docker compose up -d
```

## Conteúdo

- `docker-compose.yml` — sobe o Superset.
- `Dockerfile` — imagem customizada do Superset usada pelo compose.
