name: lab/ec2 CI/CD

on:
  pull_request:
    branches: [develop, main]
    paths:
      - "lab/ec2/**"
      - "modules/ec2/**"
      - .github/workflows/lab-ec2-ci.yml
      - .github/workflows/terraform-reusable.yml
  push:
    branches: [develop, main]
    paths:
      - "lab/ec2/**"
      - "modules/ec2/**"
      - .github/workflows/lab-ec2-ci.yml
      - .github/workflows/terraform-reusable.yml

jobs:
  terraform:
    uses: ./.github/workflows/terraform-reusable.yml
    secrets: inherit
    with:
      working_directory: lab/ec2

